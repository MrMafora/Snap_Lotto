{
  "workflows": [
    {
      "name": "Start application",
      "tasks": [
        {
          "task": "packager.installForAll"
        },
        {
          "task": "shell.exec",
          "args": "python run_with_proxy.py",
          "waitForPort": 8080,
          "description": "Run the application on port 5000 with a proxy on port 8080"
        }
      ],
      "metadata": {
        "agentRequireRestartOnSave": false
      }
    },
    {
      "name": "Port Proxy",
      "tasks": [
        {
          "task": "shell.exec",
          "args": "python port_proxy.py",
          "waitForPort": 8080,
          "description": "Run the Flask-based port proxy on port 8080"
        }
      ],
      "metadata": {
        "agentRequireRestartOnSave": false
      }
    }
  ]
}