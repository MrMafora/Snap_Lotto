{
  "workflows": [
    {
      "name": "Start application",
      "tasks": [
        {
          "task": "packager.installForAll"
        },
        {
          "task": "shell.exec",
          "args": "python direct_8080.py",
          "waitForPort": 8080,
          "description": "Start the application server directly on port 8080 with forced binding"
        }
      ],
      "metadata": {
        "agentRequireRestartOnSave": false
      }
    },
    {
      "name": "Port Proxy",
      "tasks": [
        {
          "task": "shell.exec",
          "args": "python port_proxy.py",
          "waitForPort": 8080,
          "description": "Run the Flask-based port proxy on port 8080"
        }
      ],
      "metadata": {
        "agentRequireRestartOnSave": false
      }
    }
  ]
}